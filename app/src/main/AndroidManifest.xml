<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <queries>
        <intent>
            <action android:name="com.google.android.gms.ads.service.CACHE" />
        </intent>
        <intent>
            <action android:name="com.google.android.gms.ads.service.START" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Dotto"
        android:supportsRtl="true"
        tools:targetApi="34"
        xmlns:tools="http://schemas.android.com/tools">

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-7601883112918707~5248779593" />

        <!--
            Dotto gameplay itself requires no network or storage access.
            INTERNET and ACCESS_NETWORK_STATE are used only for optional
            advertising. Gameplay remains fully functional offline.
        -->
        <activity
            android:name="com.pranav.dotto.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Dotto">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
